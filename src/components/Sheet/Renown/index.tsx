import Dottable from "@src/components/Sheet/Dottable";
import styled from "styled-components";

export const Renown = styled(Dottable)`
  .title {
    font-family: var(--bebas-neue);
    font-size: 30px;
    scale: 1 1.22;
  }
  .buttons {
    display: flex;
    align-items: center;
  }
`;
