import { styled } from "styled-components";
import LeftCaretButton from "../LeftCaretButton";
const RightCaretButton = styled(LeftCaretButton)`
  transform: rotate(180deg);
`;

export default RightCaretButton;
